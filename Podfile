# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'cuentame_ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cuentame_ios
   pod 'SwiftyJSON', '~> 4.0'
   pod 'Alamofire', '~> 4.7'
   pod 'SDWebImage', '~> 4.0'

  target 'cuentame_iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cuentame_iosUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
